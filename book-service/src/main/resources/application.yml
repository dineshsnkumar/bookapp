server:
  port: 8084

spring:
  application:
    name: book-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/bookstore
      host: localhost
      port: 27017
      username: admin
      password: password
      database: bookstore

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG